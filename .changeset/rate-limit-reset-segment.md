---
'claude-statusline-powerline': minor
---

Add a `rate_limit_reset` segment that shows the local reset time
(`HH:MM`) of a Claude.ai rate-limit window (5-hour or 7-day,
configurable via `rate_limit_reset_options.window`).
